import java.util.*;
import java.IO.*;

class DetectCyclesDFS {
    public static void main(String[] args) {
        private static int V;
        private LinkedList<Integer> adjMatrix;
    }
}

class DFS {
    private static int V;
    private LinkedList<Integer> adjMatrix;

    //constructor
    DFS(int v) {
        v = V;
        adjMatrix = new LinkedList[v];
        for(int i=0; i<v; i++) {
            adjMatrix[i] = new LinkedList()
        }
    }
}
